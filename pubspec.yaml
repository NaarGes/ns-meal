name: ns_meal
description: TheMealDb flutter application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # ui
  cupertino_icons: ^1.0.6
  shimmer: ^3.0.0

  # data
  get: ^4.6.5
  dio: ^5.3.3

  # util
  logger: ^2.0.2+1
  font_awesome_flutter: ^10.6.0
  cached_network_image: ^3.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/images/